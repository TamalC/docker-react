language: generic 
sudo: required
services: 
  - docker

before_install:
  - docker build -t tchakrab/docker-react -f Dockerfile.dev .

script:
  - docker run -e CI=true tchakrab/docker-react npm run test